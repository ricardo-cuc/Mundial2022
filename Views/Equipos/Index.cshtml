@model IEnumerable<Mundial2022.Entidades.Equipo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NEquipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ENombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UrlBandera)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NEquipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ENombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UrlBandera)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CEquipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CEquipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CEquipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
